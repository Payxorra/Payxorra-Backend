[package]
name = "reentrancy-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "21.7.7", features = ["testutils"] }
vesting-vault = { path = "../vesting-vault", features = ["testutils"] }
malicious-contract = { path = "../malicious-contract" }
