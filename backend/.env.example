# Server Configuration
PORT=4000
NODE_ENV=development

# Frontend URL — used for strict CORS (Issue #260)
FRONTEND_URL=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vesting_vault
DB_USER=postgres
DB_PASSWORD=1234

# JWT Configuration (if needed)
JWT_SECRET=your-super-secret-jwt-key-here

# External Services (if needed)
# BLOCKCHAIN_RPC_URL=https://mainnet.infura.io/v3/YOUR_PROJECT_ID
# ETHEREUM_PRIVATE_KEY=your-private-key-here

# Stellar Configuration
STELLAR_RPC_URL=https://horizon-testnet.stellar.org
STELLAR_NETWORK=testnet
STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
SOROBAN_RPC_URL=https://soroban-rpc.testnet.stellar.org
VAULT_CONTRACT_ADDRESS=CD5QF6KBAURVUNZR2EVBJISWSEYGDGEEYVH2XYJJADKT7KFOXTTIXLHU

# Off-Ramp Integration (SEP-24)
# Comma-separated list of anchors in format: domain:asset
STELLAR_ANCHORS=testanchor.stellar.org:USDC,apay.io:USDC
# Swap fee percentage for token-to-stablecoin conversion
SWAP_FEE_PERCENT=0.3

# Discord Bot Configuration
DISCORD_BOT_TOKEN=your-discord-bot-token-here
DISCORD_CHANNEL_ID=your-discord-channel-id-here

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Slack Webhook Configuration
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email Configuration
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=your-email-user
EMAIL_PASS=your-email-password
EMAIL_FROM=no-reply@vestingvault.com

# Firebase Configuration for Push Notifications
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
# Alternative: Use JSON string directly
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project",...}

# OpenTelemetry & Jaeger Configuration
OTEL_SERVICE_NAME=vesting-vault-backend
OTEL_EXPORTER_JAEGER_ENDPOINT=http://jaeger:14268/api/traces
OTEL_TRACES_SAMPLE_RATE=1.0
ENABLE_JAEGER=true
ENABLE_OTLP=false
OTEL_CONSOLE_EXPORT=false

# Field-level payload encryption for sensitive request/response fields
PAYLOAD_FIELD_ENCRYPTION_ENABLED=false
# 32-byte base64, 64-char hex, or passphrase sourced from secrets manager/HSM in production
PAYLOAD_FIELD_ENCRYPTION_KEY=
# Optional comma-separated override; defaults cover common PII and secret fields
PAYLOAD_SENSITIVE_FIELDS=email,phone,ssn,tax_id,taxId,full_name,fullName,date_of_birth,dateOfBirth,addressLine1,addressLine2,street_address,bank_account,routing_number,wallet_private_key,privateKey,secret

# Scheduled Database Backup Verification with Restore Testing (Issue #101)
# Master switch for the scheduled job (manual/API triggers still work)
BACKUP_ENABLED=true
# Local directory for encrypted backup artifacts and verification history
BACKUP_DIR=./backups
# Cron expressions: daily backup+verify at 02:00, weekly restore test Sunday 04:00
BACKUP_CRON=0 2 * * *
BACKUP_RESTORE_TEST_CRON=0 4 * * 0
BACKUP_RESTORE_TEST_ENABLED=true
# Local retention in days for encrypted artifacts
BACKUP_RETENTION_DAYS=30
# AES-256 key used to encrypt artifacts (MUST be set in production; >= 16 chars)
# BACKUP_ENCRYPTION_KEY=replace-with-a-long-random-secret
# Optional off-site copy via the aws cli, e.g. s3://lumina-backups/prod
# BACKUP_S3_BUCKET=s3://lumina-backups/prod
# BACKUP_S3_UPLOAD=false
# Key tables included in the restore-test row-count comparison
BACKUP_VERIFY_TABLES=vaults,sub_schedules,beneficiaries
# Scratch database prefix used for restore testing (created and dropped each run)
BACKUP_SCRATCH_DB_PREFIX=lumina_restore_test_
# PostgreSQL connection (shared with the app when unset)
# PG_DB=vesting_vault
# PG_USER=postgres
# PG_HOST=localhost
# PG_PORT=5432
# PG_PASSWORD=1234
